[package]
name = "annotation-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
sentenza = { path = "../../../sentenza" }
toml = "0.8"
